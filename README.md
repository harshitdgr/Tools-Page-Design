
  # Tools Page Design

  This is a code bundle for Tools Page Design. The original project is available at https://www.figma.com/design/Xzn5LoubF8X2kbSwl0W2gz/Tools-Page-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server
  
  Run 'cd band-info-backend
      go run main.go' to start the backend server
